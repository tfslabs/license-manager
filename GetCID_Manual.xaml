<Window x:Class="HGM.Hotbird64.LicenseManager.GetCID_Manual"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrls="clr-namespace:HGM.Hotbird64.LicenseManager.Controls"
        xmlns:co="clr-namespace:HGM.Hotbird64.LicenseManager.WPF.Converters"
        xmlns:local="clr-namespace:HGM.Hotbird64.LicenseManager"
        xmlns:gl="clr-namespace:System.Globalization;assembly=mscorlib"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:Icons="clr-namespace:HGM.Hotbird64.Icons"
        mc:Ignorable="d"
        Title="Get Confirmation ID (Online, Manual)"
        Loaded="GetCID_Manual_Loaded"
        ResizeMode="NoResize" Focusable="False" SizeToContent="WidthAndHeight"
        Height="180"
        Width="580">
    <Grid Width="550" Height="156" HorizontalAlignment="Center" VerticalAlignment="Top">
        <Grid Width="530" Height="26" VerticalAlignment="Top" Margin="10,10,10,0">
            <Label HorizontalAlignment="Left" Content="Phone IID" Width="62" Height="26" />
            <ctrls:WmiPropertyBox
                x:Name="PhoneInstallationIdBox"
                HorizontalAlignment="Right"
                Width="430" Height="18" IsReadOnly="False"/>
        </Grid>
        <Grid Width="530" Height="26" VerticalAlignment="Top" Margin="10,41,10,0">
            <Label HorizontalAlignment="Left" Content="Extended PID" Width="80" Height="26" />
            <ctrls:WmiPropertyBox
              x:Name="EpidBox"
              Grid.Row="5"
              Grid.Column="1"
              IsReadOnly="False"
              Margin="100,0,0,0" Width="430" Height="18"/>
        </Grid>
        <Grid Width="530" Height="24" VerticalAlignment="Top" Margin="10,72,10,0">
            <Button
                x:Name="GetCIDButton"
                HorizontalAlignment="Right"
                Click="Button_GetCID_Clicked"
                Width="60" Height="24">
                <TextBlock Margin="5,2,5,2" VerticalAlignment="Center">
			        <Run Text="Get CID"/>
                </TextBlock>
            </Button>
        </Grid>
        <Grid Width="530" Height="26" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,101,0,0">
            <Label HorizontalAlignment="Left" Content="Confirmation ID" Width="94" Height="26" />
            <TextBox
                x:Name="CIDCode"
                Margin="100,0,0,0"
                Width="430" Height="18" 
                IsReadOnly="True"/>
        </Grid>
        <Grid Margin="0,0,0,0" VerticalAlignment="Bottom" Width="550" Height="24">
            <Rectangle Fill="#f0f0f0" Width="550" Height="24" />
            <ProgressBar
                x:Name="GetCIDProgressBar"
                Margin="95,0,10,0"
                IsIndeterminate="True"
                Visibility="Visible"
                Height="16"
                VerticalAlignment="Center" Width="445" />
            <TextBlock
                x:Name="GetCIDLabelStatus"
                HorizontalAlignment="Left" Width="85" Margin="5,3,0,3" Height="18">
		        <Run Text="Initializing..." />
            </TextBlock>
        </Grid>
    </Grid>
</Window>
